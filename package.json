{
  "name": "supercarrosrd",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run dev --prefix client",
    "dev:api": "tsx scripts/dev-api.ts",
    "dev:local": "LOCAL_API=1 npm run dev --prefix client",
    "sync": "tsx scripts/sync.ts",
    "build": "tsc --noEmit && npm run build --prefix client",
    "build:client": "npm run build --prefix client"
  },
  "dependencies": {
    "@vercel/blob": "^0.27.3",
    "cheerio": "^1.0.0",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@vercel/node": "^5.1.8",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
