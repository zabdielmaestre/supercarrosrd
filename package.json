{
  "name": "supercarrosrd",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "dev:client": "npm run dev --prefix client",
    "sync": "tsx scripts/sync.ts",
    "build": "tsc --noEmit && npm run build --prefix client",
    "build:client": "npm run build --prefix client"
  },
  "dependencies": {
    "@vercel/blob": "^0.27.3",
    "cheerio": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@vercel/node": "^5.1.8",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
