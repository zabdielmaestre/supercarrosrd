# Slug del dealer en SuperCarros (ej: parraautoimport)
DEALER_SLUG=parraautoimport

# Secreto para proteger /api/sync (Vercel lo envía automáticamente en cron jobs)
CRON_SECRET=tu-secreto-aqui

# Opcional: conecta Vercel Blob en el dashboard para persistir el JSON en producción
# BLOB_READ_WRITE_TOKEN=
